version: '3.8'

services:
  whoop-classifier:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./output:/app/output
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
